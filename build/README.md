# Build Resources

`icon-placeholder.svg` is a temporary WenForge-authored placeholder. Final release icons should
replace it with platform-native `.icns`, `.ico`, and Linux PNG assets before signed distribution.
