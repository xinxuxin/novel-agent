export { DEFAULT_THEME, THEMES, normalizeTheme };
export type { ThemePreference };

import { DEFAULT_THEME, THEMES, normalizeTheme } from "@shared/theme";
import type { ThemePreference } from "@shared/theme";
